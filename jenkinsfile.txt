pipeline {
    agent { docker { image 'maven:3.3.3' } }
    stages {
        stage('Build') {
            steps {
                sh 'echo "Hello World"'
		sh '''
			ecjp "Multiline shell steps works too"
			ls -lah
            }
        }
    }
}